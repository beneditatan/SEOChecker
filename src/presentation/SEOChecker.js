class SEOChecker
{
    constructor(config)
    {
        const { inputMode, outputMode } = config;
        this.inputMode = inputMode;
        this.outputMode = outputMode;
    }
    
    setInputMode(inputMode = 0)
    {
        this.inputMode = inputMode;
    }

    setInput(htmlInput)
    {

    }

    setOutputMode(outputMode = 0)
    {
        this.outputMode = outputMode;
    }

    setRules()
    {

    }

    run()
    {

    }
}